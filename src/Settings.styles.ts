import styled from 'styled-components';

export const SettingsWrapper = styled.div`
  grid-area: 1 / 3;
  justify-self: start;
  display: flex;
  font-size: small;

  .tuning {
    margin: 0 18px 0 6px;
  }
`;
